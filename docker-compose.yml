services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ticket
      POSTGRES_PASSWORD: ticket
      POSTGRES_DB: ticket_review
    volumes:
      - ticket_pg:/var/lib/postgresql/data

volumes:
  ticket_pg:
